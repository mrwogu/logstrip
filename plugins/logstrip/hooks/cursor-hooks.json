{
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "command": "node -e \"require(process.argv.slice(1).join(' '))\" -- ${CLAUDE_PLUGIN_ROOT}/hooks/logstrip-hook.js",
        "matcher": "Read",
        "timeout": 10
      }
    ]
  }
}
